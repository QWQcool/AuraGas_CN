# AuraGas 系统架构分析

> 引擎: UE 5.8 | MCP: UE-MCP | 进度: 第7章 7.1-7.8 完成

## 项目结构

```
Source/AuraGas/
├── AbilitySystem/
│   ├── UAuraAbilitySystemComponent    ← 第7章: EffectApplied 回调
│   └── UAuraAttributeSet              ← 第6章: PreAttributeChange, PostExecute
├── Actor/
│   └── AAuraEffectActor               ← 第6章: 拾取物动画 + 三种策略
├── Character/
│   ├── AAuraCharacterBase             ← 第7章: virtual InitAbilityActorInfo()
│   ├── AAuraCharacter                 ← 第7章: AbilityActorInfoSet()
│   └── AAuraEnemy                     ← 第7章: refactor InitAbilityActorInfo
├── Player/
│   ├── AAuraPlayerController
│   └── AAuraPlayerState
└── UI/ (HUD, WidgetController, UserWidget)

Content/BP/
├── Actor/     (FireArea, Crystal, Potion 蓝图 + GE)
├── AbilitySystem/
│   └── GameplayTags/
│       └── DT_PrimaryAttributes        ← 第7章: 主属性数据表
├── Character/ (BP_Aura_Character, BP_EnemyBase, 2 Goblin)
├── Game/ / Player/ / UI/
└── Input/     (IMC_AuraContext)

Config/
└── DefaultGameplayTags.ini              ← 第7章: 23 个 GameplayTag 定义
```

## GameplayTags 体系 (第7章)

### 标签分类 (23个)

| 层级 | 示例 | 用途 |
|---|---|---|
| `Message.*` | Message.HealthPotion | 拾取物效果通知 |
| `Attributes.Vital.*` | Attributes.Vital.Health | 主属性标签映射 |
| `Abilities.*` | Abilities.Passive.ListenForEvent | 能力标签 (未来) |
| `Cooldown.*` | Cooldown.Fire.FireBlast | 冷却标签 (未来) |
| `Event.Montage.*` | Event.Montage.FireBolt | 动画标签 (未来) |
| `GameplayCue.*` | GameplayCue.MeleeImpact | 视觉特效标签 (未来) |

### 标签流

```
GE 应用 (含 AssetTags)
  ↓ UAuraASC::EffectApplied
  ↓ EffectSpec.GetAllAssetTags(TagContainer)
  ↓ for each Tag: AddOnScreenDebugMessage(Tag.ToString())
  ↓ TODO: Broadcast → WidgetController → UI
```

## C++ 继承树

```
ACharacter
├── AAuraCharacterBase (virtual InitAbilityActorInfo)
│   ├── AAuraCharacter   → AbilityActorInfoSet()
│   └── AAuraEnemy       → BeginPlay → InitAbilityActorInfo → AbilityActorInfoSet()

UAuraAbilitySystemComponent
  ├── AbilityActorInfoSet()    ← 绑定 EffectApplied 到 GE 回调
  └── EffectApplied()           ← 提取 AssetTags → 屏显调试 → TODO广播
```

## 蓝图资产

| 蓝图 | 父类 | 事件图 |
|---|---|---|
| BP_FireArea | AuraEffectActor | Box重叠→父类OnOverlap |
| BP_HealthCrystal | AuraEffectActor | Capsule重叠→OnOverlap→Destroy |
| BP_ManaCrystal | AuraEffectActor | Sphere重叠→OnOverlap→Destroy |
| BP_HealthPotion | AuraEffectActor | 14节点GAS管线 |
| BP_ManaPotion | AuraEffectActor | Box重叠→OnOverlap→Destroy |
| BP_Aura_Character | AuraCharacter | 空 |
| BP_EnemyBase | AuraEnemy | 空 |
| BP_Goblin_Slingshot | BP_EnemyBase | 空 |
| BP_Goblin_Spear | BP_EnemyBase | 空 |
| BP_AuraPlayerState | AuraPlayerState | 含ASC |
| BP_OverlayWidgetController | OverlayWidgetController | 纯C++委托 |

## GE 数据资产

| GE | Duration | AssetTag | 用途 |
|---|---|---|---|
| GE_CrystalHeal | Instant | Message.HealthCrystal | 水晶回血 |
| GE_CrystalMana | Instant | Message.ManaCrystal | 水晶回蓝 |
| GE_PotionHeal | Instant | Message.HealthPotion | 药瓶回血 |
| GE_PotionMana | Instant | Message.ManaPotion | 药瓶回蓝 |
| GE_FireArea | HasDuration | - | 持续火焰伤害 |

## 三种效果策略

| 策略 | 重叠 | 离开 |
|---|---|---|
| Instant | ApplyEffectToSelf | 无 |
| HasDuration | ApplyEffectToSelf | 移除 ActiveHandle |
| Infinite | ApplyEffectToSelf | 重叠时移除 |

## UI 控件树

```
WBP_Overlay (CanvasPanel)
├── WBP_ManaGlobe_C      ← 底部偏左
└── WBP_HealthGLobe_C    ← 底部偏右

WBP_GlobeProgressBarBase (SizeBox 250×250)
└── Overlay_Root (Image + ProgressBar + Glass)
```

## MVC 数据流

```
OnRep_Health → ASC::ValueChangeDelegate → WidgetController::HealthChanged
  → Broadcast(NewHealth) → WBP_HealthGlobe → SetPercent()
```

## 学习进度

| 章节 | 内容 | 状态 |
|---|---|---|
| 1-2 | 项目初始化 + 角色 + Enhanced Input | ✅ |
| 3-4 | GAS 属性系统 + 网络同步 | ✅ |
| 5 | UMG UI + MVC 架构 | ✅ |
| 6 | GE + EffectActor + 拾取物动画 | ✅ |
| **7.1-7.8** | **GameplayTags 体系 + ASC 回调** | **✅ 完成** |
| 7.9+ | Tag→WidgetController→UI 广播 | 进行中 |

## 技术栈

| 层级 | 技术 |
|---|---|
| GAS | ASC, AttributeSet, GameplayEffect, GameplayTag, AssetTags |
| 控制器 | EnhancedInput, PlayerController |
| UI | UMG MVC, 多播委托, GlobeProgressBar |
| 网络 | Replicated, DOREPLIFETIME_CONDITION, OnRep |
| 工具链 | UE-MCP (111 tools), CodeBuddy |
| 引擎 | UE 5.8 (从 5.6 升级) |
