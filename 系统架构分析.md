# AuraGas 系统架构分析

> 引擎: UE 5.8 (从 5.6 升级) | MCP: UE-MCP | 进度: 第8章完结（1~8 章完成）
>
> 本文档融合了早期逐章分析（1-6 章详细内容曾于 7-21 文档重写时丢失，已从 `5f14de6` 历史版本恢复）与第 7-8 章结构化分析。

## 系统架构概览

```
GAS 系统层
├── UAuraAttributeSet                  ← 属性管理（数据模型）
├── UAuraAbilitySystemComponent        ← 能力系统组件（回调广播）
├── AAuraCharacterBase                 ← 角色基类（集成 GAS + 战斗接口）
├── UMMC_MaxHealth / UMMC_MaxMana      ← 自定义计算（次级属性派生）
└── ULevelUpInfo (DataAsset)           ← 升级经验表

UI 系统层（MVC）
├── AAuraHUD                           ← 界面管理器（Controller 工厂）
├── AAuraWidgetController              ← 控制器基类
└── AOverlayWidgetController           ← 覆盖层控制器（多播委托）

玩家状态层
├── AAuraPlayerState                   ← Level/XP/AttributePoints/SpellPoints + 复制委托
└── ICombatInterface                   ← GetPlayerLevel() 解耦

蓝图层
├── BP_AuraHUD / BP_OverlayWidgetController
├── WBP_Overlay / WBP_HealthGlobe / WBP_ManaGlobe
├── BP_Aura_Character / BP_EnemyBase / Goblin 系列
└── BP_AuraPlayerState / BP_TestActor
```

## 项目结构

```
Source/AuraGas/
├── AbilitySystem/
│   ├── UAuraAbilitySystemComponent    ← 第7章: EffectApplied 回调
│   ├── UAuraAttributeSet              ← 第8章: 20 属性 + PreAttributeChange/PostExecute
│   ├── Data/
│   │   └── ULevelUpInfo               ← 第8章: 等级经验表 DataAsset + FindLevelForXP
│   └── ModMagCalc/
│       ├── UMMC_MaxHealth             ← 第8章: 80+2.5×Vigor+10×Level
│       └── UMMC_MaxMana               ← 第8章: 50+2.5×Int+15×Level
├── Actor/
│   └── AAuraEffectActor               ← 第6章: 拾取物动画 + 三种策略
├── Character/
│   ├── AAuraCharacterBase             ← 第8章: InitializeDefaultAttributes 三级 GE 链
│   ├── AAuraCharacter                 ← 第7章: AbilityActorInfoSet()
│   └── AAuraEnemy                     ← 第7章: refactor InitAbilityActorInfo
├── Interaction/
│   ├── IEnemyInterface                ← 第4章: HighlightActor/UnHighlightActor
│   └── ICombatInterface               ← 第8章: GetPlayerLevel() 接口
├── Player/
│   ├── AAuraPlayerController
│   └── AAuraPlayerState               ← 第8章: Level/XP/AttrPts/SpellPts + 复制委托
├── AuraGameplayTags                    ← 第8章: 原生标签单例 (~60 标签)
└── UI/
    ├── HUD/AAuraHUD                   ← 界面管理器（InitOverlay 工厂）
    ├── Widget/                         ← UMG 控件（WBP_Overlay/Globe 等）
    └── WidgetController/
        ├── AAuraWidgetController      ← 控制器基类（FWidgetControllerParams）
        └── AOverlayWidgetController   ← 覆盖层控制器（Health/Mana 委托）

Content/BP/
├── Actor/     (FireArea, Crystal, Potion 蓝图 + GE, TestActor)
├── AbilitySystem/
│   ├── GameplayTags/
│   │   ├── DT_PrimaryAttributes        ← 第7章: 主属性数据表
│   │   └── DT_InitialPrimaryValues     ← 第8章: 主属性初始值表
│   └── Data/
│       └── DA_LevelUpInfo              ← 第8章: 升级经验表资产
├── Character/
│   ├── Aura/ (BP_Aura_Character)       ← 第8章: 挂 3 个 Default* GE
│   └── Enemy/ (BP_EnemyBase, 2 Goblin)
├── Game/ / Player/ / UI/
└── Input/     (IMC_AuraContext)

Config/
└── DefaultGameplayTags.ini              ← 第7章: 23 个 GameplayTag 定义
```

## C++ 核心类职责详解

### GAS 系统核心文件

#### `AuraAttributeSet.h/cpp`
**作用**: 游戏属性管理系统（第3章引入，第8章扩展为 20 属性）
- **核心功能**: 定义和管理角色全部属性（血量、法力、主/次/抗性/Meta）
- **技术特点**:
  - 使用 GAS 宏 `ATTRIBUTE_ACCESSORS` 简化属性访问器定义
  - 网络复制（`ReplicatedUsing` + `GetLifetimeReplicatedProps`）
  - 属性变更回调 `OnRep_*`、`PreAttributeChange`、`PostGameplayEffectExecute`（8.x clamp）
- **关键方法**:
  - `OnRep_Health()/OnRep_Mana()`: 属性网络同步回调
  - `PostGameplayEffectExecute()`: 伤害结算、XP 结算中转（Meta 属性）

#### `AuraCharacterBase.h/cpp`
**作用**: 角色基类，集成 GAS 系统（第3章）
- **核心功能**: 为所有角色提供 GAS 支持 + 战斗接口
- **技术特点**:
  - 实现 `IAbilitySystemInterface` + `ICombatInterface`（第8章）
  - 持有 `AbilitySystemComponent` / `AttributeSet` 指针 + `Weapon` 武器网格
  - 抽象类（`UCLASS(Abstract)`），支持继承扩展
- **关键方法**:
  - `InitAbilityActorInfo()`: 虚函数，子类各自初始化（虚拟代理）
  - `InitializeDefaultAttributes()`: 第8章三级 GE 初始化链
  - `ApplyEffectToSelf()`: 通用 GE 应用封装

#### `AuraAbilitySystemComponent.h/cpp`
**作用**: 能力系统组件（第3章引入，第7章增强）
- **核心功能**: ASC 生命周期管理 + GE 应用回调
- **关键方法**:
  - `AbilityActorInfoSet()`: 绑定 `EffectApplied` 回调
  - `EffectApplied()`: 提取 AssetTags → 屏显调试 → 广播 UI（7.14 消息系统）

### UI 系统核心文件

#### `AuraHUD.h/cpp`
**作用**: 游戏界面管理器（第5章 MVC）
- **核心功能**: 管理 UI 控件与 WidgetController 的创建
- **技术特点**: 继承 `AHUD`；`TSubclassOf` 类型安全；动态控件创建
- **关键方法**:
  - `InitOverlay()`: 初始化覆盖层 UI 系统
  - `GetOverlayWidgetController()`: 获取或创建控制器（单例工厂）

#### `AuraWidgetController.h/cpp`
**作用**: UI 控制器基类（第5章）
- **核心功能**: UI 与 GAS 系统的桥梁
- **技术特点**: `UObject` 非场景对象；`FWidgetControllerParams` 参数结构体；虚函数多态
- **关键方法**:
  - `BroadcastInitialValues()`: 广播初始属性值
  - `BindCallBacksToDependencies()`: 绑定属性变更回调

#### `OverlayWidgetController.h/cpp`
**作用**: 覆盖层 UI 控制器（第5章）
- **核心功能**: 血/蓝量属性同步到 WBP_Overlay
- **技术特点**: 多播委托（`OnHealthChanged`/`OnMaxHealthChanged`/`OnManaChanged`/`OnMaxManaChanged`）；`Blueprintable/BlueprintType`

### 玩家状态层

#### `AuraPlayerState.h/cpp`
**作用**: 玩家状态（第8章，GAS 归属从 Character 迁移至 PlayerState）
- **核心功能**: 持有 ASC/AttributeSet + 玩家成长数据
- **技术特点**: 4 个复制属性 + OnRep 委托广播（详见"玩家状态系统"节）

## 接口系统深度分析

### IAbilitySystemInterface（UE 引擎标准接口）

```cpp
// UE 引擎内置的 GAS 核心接口
class IAbilitySystemInterface
{
public:
    virtual UAbilitySystemComponent* GetAbilitySystemComponent() const = 0;
};
```

**为什么需要这个接口？**
1. **统一访问入口**: 为所有支持 GAS 的对象提供标准化访问方式
2. **类型安全**: 通过接口而非直接类型转换，避免运行时错误
3. **多态支持**: 允许不同类型的对象（角色、玩家状态）统一实现
4. **接口隔离**: 解耦具体实现

**在 AuraGas 项目中的实现**（第8章重构后）

```cpp
// AuraCharacterBase.h - 角色基类实现
class AAuraCharacterBase : public ACharacter, public IAbilitySystemInterface, public ICombatInterface
{
    virtual UAbilitySystemComponent* GetAbilitySystemComponent() const override;
};

// AuraPlayerState.h - 玩家状态实现（实际拥有 ASC）
class AAuraPlayerState : public APlayerState, public IAbilitySystemInterface
{
    virtual UAbilitySystemComponent* GetAbilitySystemComponent() const override;
};
```

**具体使用场景**（EffectActor 安全获取 ASC）

```cpp
void AAuraEffectActor::OnOverlap(AActor* OtherActor)
{
    if (IAbilitySystemInterface* ASCInterface = Cast<IAbilitySystemInterface>(OtherActor))
    {
        UAbilitySystemComponent* ASC = ASCInterface->GetAbilitySystemComponent();
        // 安全地应用游戏效果
    }
}
```

### IEnemyInterface（第4章）

```cpp
// EnemyInterface.h - 自定义敌人交互接口
class IEnemyInterface
{
public:
    virtual void HighlightActor() = 0;    // 高亮显示敌人
    virtual void UnHighlightActor() = 0;  // 取消高亮
};
```

**用途**: 鼠标悬停高亮检测的统一抽象，不依赖具体实现。

**在 AuraGas 项目中的实现**（`AAuraEnemy`）

```cpp
void AAuraEnemy::HighlightActor()
{
    GetMesh()->SetRenderCustomDepth(true);
    GetMesh()->SetCustomDepthStencilValue(CUSTOM_DEPTH_RED);  // 红色描边
    Weapon->SetRenderCustomDepth(true);
}
void AAuraEnemy::UnHighlightActor()
{
    GetMesh()->SetRenderCustomDepth(false);
    Weapon->SetRenderCustomDepth(false);
}
```

**使用场景**（`AuraPlayerController::CursorTrace`，第4章鼠标检测）

```cpp
IEnemyInterface* LastEnemyInterface = Cast<IEnemyInterface>(LastActor);
IEnemyInterface* ThisEnemyInterface = Cast<IEnemyInterface>(ThisActor);
if (ThisEnemyInterface) ThisEnemyInterface->HighlightActor();
if (LastEnemyInterface) LastEnemyInterface->UnHighlightActor();
```

### ICombatInterface（第8章）

```cpp
// CombatInterface.h - 战斗/成长数据解耦接口
class ICombatInterface
{
public:
    virtual int32 GetPlayerLevel();
};
```

**用途**: 让 MMC 计算（MaxHealth/MaxMana）不依赖具体角色类即可读取等级，解耦"属性派生"与"角色类型"。

**实现**:
| 实现类 | 实现方式 |
|---|---|
| `AAuraCharacterBase`（第8章） | 从 PlayerState 获取 `GetPlayerLevel()` |
| `AAuraEnemy` | 直接返回 1（敌人无成长等级） |

### 接口设计原则应用

1. **接口隔离原则（ISP）**: `IAbilitySystemInterface` 只关注 GAS；`IEnemyInterface` 只关注交互；`ICombatInterface` 只关注战斗成长数据——职责单一
2. **依赖倒置原则（DIP）**: 高层（PlayerController/MMC）依赖抽象接口而非具体实现
3. **开闭原则（OCP）**: 新增敌人类型只需实现接口，不影响鼠标检测/属性计算逻辑

### 接口使用最佳实践

```cpp
// 推荐：用接口安全类型检查
if (IEnemyInterface* enemy = Cast<IEnemyInterface>(actor)) { enemy->HighlightActor(); }
// 不推荐：直接依赖具体类型
if (AAuraEnemy* enemy = Cast<AAuraEnemy>(actor)) { /* 不通用 */ }
```

- `UINTERFACE(MinimalAPI)` 宏确保接口在蓝图中可用
- 接口基类（UEnemyInterface/UCombatInterface）为 `UInterface`，实际接口为 `I*Interface`

## GameplayTags 体系

### 标签分类（原生单例 + ini）

| 层级 | 示例 | 用途 |
|---|---|---|
| `Message.*` | Message.HealthPotion | 拾取物效果通知 |
| `Attributes.Primary.*` | Attributes.Primary.Strength | 主属性映射 |
| `Attributes.Secondary.*` | Attributes.Secondary.Armor | 次级属性映射 |
| `Attributes.Resistance.*` | Attributes.Resistance.Fire | 抗性映射 |
| `Attributes.Meta.*` | Attributes.Meta.IncomingXP | 元属性映射 |
| `Damage.*` | Damage.Fire / Damage.Physical | 伤害类型 |
| `Debuff.*` | Debuff.Burn + Chance/Damage/Duration/Frequency | 减益 (未来) |
| `Abilities.*` | Status/Type/Fire.FireBolt/Passive.* | 技能系统 (未来) |
| `Cooldown.*` | Cooldown.Fire.FireBolt | 冷却标签 (未来) |
| `InputTag.*` | InputTag.LMB / InputTag.1 | 输入绑定 (未来) |
| `Player.Block.*` | Player.Block.InputPressed | 输入屏蔽 (未来) |
| `CombatSocket.*` / `Montage.Attack.*` | 武器槽 / 攻击动画 (未来) |
| `GameplayCue.*` | GameplayCue.FireBlast | 视觉特效 (未来) |

> 第7章: ini 定义基础 Message/Attributes；第8章: 原生单例 `FAuraGameplayTags::InitializeNativeGameplayTags()` 全量注册。

## 属性体系 (第8章)

### 四类属性 + 两级 Meta

| 分类 | 属性 | 复制 | 来源 |
|---|---|---|---|
| **Primary** | Strength / Intelligence / Resilience / Vigor | ✅ | GE_AuraPrimaryBlueprint (Instant + DT 查表) |
| **Secondary** | Armor / ArmorPen / BlockChance / CritChance / CritDamage / CritResist / HealthRegen / ManaRegen / **MaxHealth / MaxMana** | ✅ | GE_SecondaryAttributes (Infinite) |
| **Resistance** | Fire / Lightning / Arcane / Physical | ✅ | GE_SecondaryAttributes |
| **Vital** | Health / Mana | ✅ | GE_VitalAttributes (Instant: Health=MaxHealth) |
| **Meta** | IncomingDamage / IncomingXP | ❌ | 瞬时伤害/经验中转 |

### 属性派生链

```
Strength/Intelligence/Resilience/Vigor   [GE_AuraPrimaryBlueprint]
        ↓ MMC / AttributeBased           [GE_SecondaryAttributes, Infinite, bSnapshot=false]
MaxHealth = 80 + 2.5×Vigor + 10×Level    [MMC_MaxHealth]
MaxMana   = 50 + 2.5×Int  + 15×Level     [MMC_MaxMana]
Armor = 0.25×(Resilience+2)+6           ArmorPen = 0.15×(Resilience+1)+16
BlockChance = 0.25×Armor+4              CritChance = 0.5×ArmorPen+10
CritResist = 0.25×Armor+10              CritDamage = 0.25×ArmorPen+5
HealthRegen = 0.1×Vigor+1               ManaRegen = 0.1×Int+1
四系抗性 = 0.5×Resilience+3
        ↓ Override Coefficient=1         [GE_VitalAttributes, Instant]
Health = MaxHealth                       Mana = MaxMana
```

### 初始化串

```
AAuraCharacterBase::InitializeDefaultAttributes()   [BeginPlay 时]
  ├── ApplyEffectToSelf(DefaultPrimaryAttributes)
  ├── ApplyEffectToSelf(DefaultSecondaryAttributes)
  └── ApplyEffectToSelf(DefaultVitalAttributes)
```

## 玩家状态系统 (8.11)

```
AAuraPlayerState (网络复制)
├── int32 Level = 1           → DOREPLIFETIME → OnRep_Level → OnLevelChangedDelegate(Level, bLevelUp)
├── int32 XP   = 0            → DOREPLIFETIME → OnRep_XP   → OnXPChangedDelegate
├── int32 AttributePoints = 0 → DOREPLIFETIME → OnRep_AttrPts → OnAttributePointsChangedDelegate
└── int32 SpellPoints = 0     → DOREPLIFETIME → OnRep_SpellPts → OnSpellPointsChangedDelegate

AddTo* / Set* 方法 → 值变更 + 委托广播 → MMC 读取 PlayerLevel / UI 订阅刷新
```

**ULevelUpInfo DataAsset**（`DA_LevelUpInfo`）: `FAuraLevelUpInfo` 数组，`FindLevelForXP(XP)` 循环查表返回目标等级。

## C++ 继承树

```
ACharacter
├── AAuraCharacterBase (abstract: IAbilitySystemInterface + ICombatInterface, virtual InitAbilityActorInfo, InitializeDefaultAttributes)
│   ├── AAuraCharacter   → AbilityActorInfoSet()
│   └── AAuraEnemy       → BeginPlay → InitAbilityActorInfo → AbilityActorInfoSet(), IEnemyInterface

APlayerState
└── AAuraPlayerState : IAbilitySystemInterface
    ├── 持有 ASC (Full 复制) + AttributeSet
    └── Level/XP/AttributePoints/SpellPoints (DOREPLIFETIME + OnRep 委托)

UAuraAbilitySystemComponent
  ├── AbilityActorInfoSet()    ← 绑定 EffectApplied 到 GE 回调
  └── EffectApplied()           ← 提取 AssetTags → 屏显调试 → 广播 UI

UGameplayModMagnitudeCalculation
├── UMMC_MaxHealth             ← 捕获 Vigor, 公式含 PlayerLevel
└── UMMC_MaxMana               ← 捕获 Intelligence, 公式含 PlayerLevel

UDataAsset
└── ULevelUpInfo               ← FAuraLevelUpInfo 表 + FindLevelForXP()

ICombatInterface
  ├── AAuraCharacter           → GetPlayerLevel() = PlayerState->GetPlayerLevel()
  └── AAuraEnemy               → GetPlayerLevel() = 1
```

## 系统交互流程

### 属性更新流程

```
GAS 属性变更 → AttributeSet.OnRep → ASC::ValueChangeDelegate →
WidgetController 属性回调 → 委托广播 → UI 控件更新显示
```

### UI 初始化流程（第5章 MVC）

```
游戏开始 → AuraHUD.BeginPlay → InitOverlay →
创建 WidgetController (FWidgetControllerParams) → BroadcastInitialValues →
BindCallBacksToDependencies → 显示 WBP_Overlay（含血/蓝球）
```

### 标签流（第7章）

```
GE 应用 (含 AssetTags)
  ↓ UAuraASC::EffectApplied
  ↓ EffectSpec.GetAllAssetTags(TagContainer)
  ↓ for each Tag: AddOnScreenDebugMessage(Tag.ToString())
  ↓ (7.14) Broadcast → WidgetController → UI 拾取提示
```

## 蓝图资产

| 蓝图 | 父类 | 事件图 |
|---|---|---|
| BP_FireArea | AuraEffectActor | Box重叠→父类OnOverlap |
| BP_HealthCrystal | AuraEffectActor | Capsule重叠→OnOverlap→Destroy |
| BP_ManaCrystal | AuraEffectActor | Sphere重叠→OnOverlap→Destroy |
| BP_HealthPotion | AuraEffectActor | 14节点GAS管线 |
| BP_ManaPotion | AuraEffectActor | Box重叠→OnOverlap→Destroy |
| BP_Aura_Character | AuraCharacter | 挂 3 个 Default* GE |
| BP_EnemyBase | AuraEnemy | 空 |
| BP_Goblin_Slingshot | BP_EnemyBase | 空 |
| BP_Goblin_Spear | BP_EnemyBase | 空 |
| BP_AuraPlayerState | AuraPlayerState | 含ASC |
| BP_OverlayWidgetController | OverlayWidgetController | 纯C++委托 |
| BP_TestActor | Actor | 属性派生测试 |

### UI 蓝图用途

| 蓝图 | 用途 |
|---|---|
| BP_AuraHUD | 配置 OverlayWidgetClass / OverlayWidgetControllerClass（避免硬编码） |
| BP_OverlayWidgetController | 蓝图可访问的控制器实例 |
| WBP_Overlay | 主界面：组合血条、法力条、技能栏等 |
| WBP_HealthGlobe / WBP_ManaGlobe | 继承 WBP_GlobeProgressBarBase 的圆形进度条 |

## GE 数据资产

| GE | Duration | Modifier 方式 | 用途 |
|---|---|---|---|
| GE_CrystalHeal | Instant | SetByCaller | 水晶回血 |
| GE_CrystalMana | Instant | SetByCaller | 水晶回蓝 |
| GE_PotionHeal | Instant | SetByCaller | 药瓶回血 |
| GE_PotionMana | Instant | SetByCaller | 药瓶回蓝 |
| GE_FireArea | HasDuration | SetByCaller | 持续火焰伤害 |
| GE_AuraPrimaryBlueprint | Instant | 查 DT 表 | 主属性初始化 |
| GE_SecondaryAttributes | Infinite | **MMC + AttributeBased** | 次级属性派生 |
| GE_VitalAttributes | Instant | AttributeBased (Override, Coef=1) | Health/Mana 初始化 |
| GE_TestAttributeBased | Infinite | AttributeBased | 属性派生测试 |

## 三种效果策略（第6章）

| 策略 | 重叠 | 离开 |
|---|---|---|
| Instant | ApplyEffectToSelf | 无 |
| HasDuration | ApplyEffectToSelf | 移除 ActiveHandle |
| Infinite | ApplyEffectToSelf | 重叠时移除 |

## UI 控件树

```
WBP_Overlay (CanvasPanel)
├── WBP_ManaGlobe_C      ← 底部偏左
└── WBP_HealthGLobe_C    ← 底部偏右

WBP_GlobeProgressBarBase (SizeBox 250×250)
└── Overlay_Root (Image + ProgressBar + 幽灵球尾迹 + Glass)
```

## MVC 数据流

```
OnRep_Health → ASC::ValueChangeDelegate → WidgetController::HealthChanged
  → Broadcast(NewHealth) → WBP_HealthGlobe → SetPercent()
```

## 设计模式应用

| 模式 | 体现 |
|---|---|
| **MVC** | Model=`AuraAttributeSet` / View=`WBP_*` / Controller=`*WidgetController` |
| **观察者** | 主题=GAS 属性变更；观察者=WidgetController 委托绑定；值变化自动更新 UI |
| **模板方法** | `AuraWidgetController` 定义骨架（BroadcastInitialValues/BindCallBacks）；子类实现细节 |
| **接口隔离** | IAbilitySystemInterface / IEnemyInterface / ICombatInterface 职责单一 |
| **策略** | PlayerController 鼠标检测：不同敌人类型实现同一接口，统一调用 `HighlightActor()` |
| **适配器** | 各敌人类型适配到统一接口，客户端只与接口交互 |
| **工厂（单例化）** | `AuraHUD::GetOverlayWidgetController()` 获取或创建控制器 |

## 技术亮点总结

### 架构优势
1. **松耦合设计**: UI 与 GAS 通过 WidgetController 解耦
2. **可扩展性**: 基类 + 接口设计支持功能扩展（Enemy/Goblin 系列）
3. **网络同步**: 完整属性复制机制（ReplicatedUsing/DOREPLIFETIME/OnRep）
4. **蓝图集成**: C++ 与蓝图无缝协作（UINTERFACE、Blueprintable）

### UE5 特性应用
1. **Enhanced Input**: 现代化输入系统
2. **GAS**: 专业能力系统框架（ASC/AttributeSet/GE/MMC/AttributeBased）
3. **TObjectPtr**: 类型安全对象指针
4. **反射系统**: 完整蓝图支持

### 开发效率
1. **宏简化**: `ATTRIBUTE_ACCESSORS` 减少重复代码
2. **参数结构体**: `FWidgetControllerParams` 统一参数传递
3. **委托系统**: 简化事件通知机制（多播委托 + OnRep）
4. **蓝图可视化**: 降低逻辑实现难度

## 学习进度

| 章节 | 内容 | 状态 |
|---|---|---|
| 1-2 | 项目初始化 + 角色 + Enhanced Input | ✅ |
| 3-4 | GAS 属性系统 + 网络同步 + 鼠标高光 | ✅ |
| 5 | UMG UI + MVC 架构 | ✅ |
| 6 | GE + EffectActor + 拾取物动画 | ✅ |
| 7 | GameplayTags + 消息系统 + 幽灵球 | ✅ |
| **8** | **属性初始化 + 派生链 + 玩家状态** | **✅ 完成** |
| 9 | Attribute Menu: 属性点分配 UI | 下一步 |

## 技术栈

| 层级 | 技术 |
|---|---|
| GAS | ASC, AttributeSet, GameplayEffect, GameplayTag, MMC, AttributeBased, AssetTags |
| 属性 | Primary→Secondary→Vital 三级派生链, Meta 中转 |
| 状态 | PlayerState + DOREPLIFETIME + OnRep 委托, DataAsset 经验表 |
| 控制器 | EnhancedInput, PlayerController |
| UI | UMG MVC, 多播委托, GlobeProgressBar |
| 网络 | Replicated, DOREPLIFETIME_CONDITION, OnRep |
| 工具链 | UE-MCP, CodeBuddy |
| 引擎 | UE 5.8 (从 5.6 升级) |
