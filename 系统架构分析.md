# AuraGas 系统架构分析

> 引擎版本: UE 5.8 (从 5.6 升级) | MCP 工具: UE-MCP (db-lyon) | 最后更新: 2026-07-21

## 项目模块结构

```
AuraGas.uproject
├── Source/AuraGas/                    # C++ 主模块 (15 .h + 15 .cpp)
│   ├── Public/
│   │   ├── AbilitySystem/             # UAuraAbilitySystemComponent, UAuraAttributeSet
│   │   ├── Actor/                     # AAuraEffectActor
│   │   ├── Character/                 # AAuraCharacterBase, AAuraCharacter, AAuraEnemy
│   │   ├── Game/                      # AAuraGameModeBase
│   │   ├── Interaction/               # IEnemyInterface
│   │   ├── Player/                    # AAuraPlayerController, AAuraPlayerState
│   │   └── UI/
│   │       ├── HUD/                   # AAuraHUD
│   │       ├── Widget/                # UAuraUserWidget
│   │       └── WidgetController/      # UAuraWidgetController, UOverlayWidgetController
│   └── Private/ (同行文件结构)
│
├── Content/BP/                        # 蓝图资产
│   ├── Actor/
│   │   ├── Area/                      # BP_FireArea + GE_FireArea
│   │   ├── Crystal/                   # BP_HealthCrystal, BP_ManaCrystal + GE
│   │   └── Potion/                    # BP_HealthPotion, BP_ManaPotion + GE
│   ├── Character/                     # BP_Aura_Character, BP_EnemyBase, AnimBP
│   ├── Game/                          # BP_AuraGameMode
│   ├── Input/                         # IMC_AuraContext
│   ├── Player/                        # BP_AuraPlayerController, BP_AuraPlayerState
│   └── UI/
│       ├── HUD/                       # BP_AuraHUD, WBP_MainMenu
│       ├── Overlay/                   # WBP_Overlay (主覆盖层)
│       ├── ProgressBar/               # WBP_GlobeProgressBarBase → Health/Mana 球
│       └── WidgetController/          # BP_OverlayWidgetController
│
├── Content/{Assets,Maps,Collections,Developers}/
├── Plugins/
│   ├── UE_MCP_Bridge/                 # UE-MCP C++ 桥接 (569+ 工具)
│   └── Developer/RiderLink/           # Rider IDE 支持
└── Source/AuraGas{Editor}.Target.cs   # V7 + bOverrideBuildEnvironment + Unreal5_8
```

## C++ 类继承层次

```
ACharacter
├── AAuraCharacterBase (Abstract + IAbilitySystemInterface)
│   ├── AAuraCharacter              # 玩家角色
│   └── AAuraEnemy                  # 敌人 + IEnemyInterface

APlayerController → AAuraPlayerController    # EnhancedInput + 鼠标高光
APlayerState     → AAuraPlayerState          # IAbilitySystemInterface

UAbilitySystemComponent → UAuraAbilitySystemComponent

UAttributeSet → UAuraAttributeSet
  Health: 50 / MaxHealth: 100   (DOREPLIFETIME_CONDITION, COND_None)
  Mana:   10 / MaxMana:   50    (同上)

UObject
├── UAuraWidgetController           # MVC Controller 基类
│   └── UOverlayWidgetController    # Health/Mana 多播委托绑定
└── UAuraUserWidget   (SetWidgetController)

AActor → AAuraEffectActor
  TArray<FEffectBucket> EffectBuckets;  # 按 Instant/Duration/Infinite 分桶
  ApplyEffectToTarget / OnOverlap / EndOverlap
```

## 蓝图 → C++ 映射

| 蓝图 | C++ 父类 | 用途 |
|---|---|---|
| BP_Aura_Character | AAuraCharacter | 玩家实例 |
| BP_EnemyBase / BP_Goblin_* | AAuraEnemy | 敌人 |
| BP_AuraGameMode | AAuraGameModeBase | 游戏模式 |
| BP_AuraPlayerController | AAuraPlayerController | 控制器 |
| BP_AuraPlayerState | AAuraPlayerState | 玩家状态 |
| BP_AuraHUD | AAuraHUD | HUD 管理器 |
| BP_OverlayWidgetController | UOverlayWidgetController | UI 控制器 |
| WBP_Overlay | UAuraUserWidget | 主覆盖层 |
| BP_HealthPotion / BP_ManaPotion | AAuraEffectActor | 药瓶（Instant GE） |
| BP_HealthCrystal / BP_ManaCrystal | AAuraEffectActor | 水晶（Instant GE） |
| BP_FireArea | AAuraEffectActor | 火焰区域（持续伤害） |

## UI 控件树

### WBP_Overlay — 主界面

```
CanvasPanel_42 (CanvasPanel)
├── WBP_ManaGlobe_C      ← 锚定: 底部中心偏左 (X=-800)
└── WBP_HealthGLobe_C    ← 锚定: 底部中心偏右 (X=547)
    共同锚点: (0.5, 1.0) 底部中心
```

### WBP_GlobeProgressBarBase — 球型进度条

```
SizeBox_Root (250×250, Padding=10)
└── Overlay_Root (3 层叠放)
    ├── Image_Background    ← GlobeRing.png (597×596)
    ├── ProgressBar_Globe   ← MI_HealthGlobe / MI_ManaGlobe
    └── Image_Glass         ← MI_EmptyGlobe (玻璃罩)
```

| 派生类 | Fill Material | 绑定变量 |
|---|---|---|
| WBP_HealthGLobe | `MI_HealthGlobe` | `Health` / `Max Health` |
| WBP_ManaGlobe | `MI_ManaGlobe` | `Mana` / `MaxMana` |

## BP_FireArea 详细结构

### 组件树
```
SceneRoot (SceneComponent) [native]
├── Box (BoxComponent)              ← 碰撞检测
│   └── Fire_2 (NiagaraComponent)   ← 小火焰粒子
└── FireEffect (NiagaraComponent)   ← 大火焰粒子
```

### EventGraph — 7 节点, 2 条执行流

```
[禁用] BeginPlay (0,0)
[禁用] ActorBeginOverlap (0,208)
[禁用] Tick (0,416)

启用节点:
Box.OnComponentBeginOverlap ──then──→ OnOverlap(TargetActor=OtherActor)
        OtherActor ──────────────────→ TargetActor (数据线)

Box.OnComponentEndOverlap ──then──→ OnEndOverlap(TargetActor=OtherActor)
        OtherActor ─────────────────→ TargetActor (数据线)
```

### GE_FireArea
纯 `GameplayEffect` 数据资产，伤害周期/数值在 Duration Policy / Modifiers 面板配置，逻辑走父类 `AuraEffectActor`。

## MVC 数据流

```
UAuraAttributeSet::OnRep_Health()
  ↓
ASC::GetGameplayAttributeValueChangeDelegate
  ↓
UOverlayWidgetController::HealthChanged(FOnAttributeChangeData)
  ↓
FOnHealthChangedSignature.Broadcast(NewHealth)
  ↓
WBP_HealthGlobe → ProgressBar_Globe.SetPercent()
```

## 三种效果策略 (AuraEffectActor)

| 策略 | 碰撞行为 | 离开行为 |
|---|---|---|
| Instant | 立即 ApplyEffectToSelf | 无 |
| HasDuration | ApplyEffectToSelf | 移除 ActiveHandle |
| Infinite | ApplyEffectToSelf | 再次重叠时移除（防重复） |

## UE 5.6 → 5.8 升级变更

| 文件 | 变更 |
|---|---|
| `AuraEffectActor.cpp` | `.Data.Get()` → `Data`, `.Def.Get()` → `Def` |
| `OverlayWidgetController.h` | `#include "GameplayEffectTypes.h"`, 移除 struct 前向声明 |
| `AuraAttributeSet.cpp` | `DOREPLIFETIME_CONDITION_NOTIFY` → `DOREPLIFETIME_CONDITION` |
| `AuraPlayerState.cpp` | `Mixed` → `Full` |
| `AuraPlayerController.cpp` | `ECC_Visibility` → `ECollisionChannel::ECC_Visibility` |
| `*.Target.cs` | `V5`→`V7`, `bOverrideBuildEnvironment=true`, `IncludeOrderVersion=Unreal5_8` |

## MCP 工具链

| 阶段 | 工具 | 状态 |
|---|---|---|
| v0 | Unreal_mcp v0.5.30 (ChiR24) | 已移除 — `/Game/BP` Asset Registry 不可见 |
| v1 | UE 5.8 官方 MCP + All Toolsets | 已启用 — BlueprintTools 发现但不支持 `call_tool` |
| v2 | **UE-MCP (db-lyon)** | ✅ 当前使用 — 111 工具, 蓝图变量/函数/事件深度可读 |

### UE-MCP 核心能力

| 工具 | 读/写 | 能力 |
|---|---|---|
| `blueprint.read` | 读 | 组件树 (含 native) + 继承链 |
| `blueprint.list_variables` | 读 | 变量名+类型+默认值 |
| `blueprint.read_graph` | 读 | 节点级 (引脚/连线/坐标/默认值) |
| `blueprint.read_graph_summary` | 读 | 轻量图摘要 |
| `blueprint.get_execution_flow` | 读 | 追踪 exec 流 |
| `widget.read_tree` | 读 | UMG 控件树 (含 slot 锚点) |
| `widget.get_properties` | 读 | 控件属性 (含子控件递归) |
| `gas.*` | 读写 | GAS 属性/效果/技能 |
| `blueprint.create` 等 | 写 | 创建蓝图/加节点/连引脚/编译 |

