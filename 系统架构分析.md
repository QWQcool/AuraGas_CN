# AuraGas 系统架构分析

> 引擎: UE 5.8 | MCP: UE-MCP 111 tools | 更新: 2026-07-21

## 项目结构

```
AuraGas.uproject
├── Source/AuraGas/ (15 .h + 15 .cpp)
│   ├── AbilitySystem/  UAuraASC, UAuraAttributeSet
│   ├── Actor/          AAuraEffectActor
│   ├── Character/      AAuraCharacterBase, AAuraCharacter, AAuraEnemy
│   ├── Game/           AAuraGameModeBase
│   ├── Interaction/    IEnemyInterface
│   ├── Player/         AAuraPlayerController, AAuraPlayerState
│   └── UI/             AAuraHUD, UAuraWidgetController, UOverlayWidgetController
│
├── Content/BP/ (15 蓝图 + 8 动画蓝图 + 5 GE + 1 输入映射)
│   ├── Actor/Area/          BP_FireArea + GE_FireArea
│   ├── Actor/Crystal/       BP_HealthCrystal, BP_ManaCrystal + 2 GE
│   ├── Actor/Potion/        BP_HealthPotion, BP_ManaPotion + 2 GE
│   ├── Character/           BP_EnemyBase, ABP_Enemy
│   ├── Character/Aura/      BP_Aura_Character, ABP_Aura
│   ├── Character/Goblin_SlingShot/  BP_Goblin_Slingshot + ABP
│   ├── Character/Goblin_Spear/      BP_Goblin_Spear + ABP
│   ├── Game/                BP_AuraGameMode
│   ├── Input/               IMC_AuraContext
│   ├── Player/              BP_AuraPlayerController, BP_AuraPlayerState
│   └── UI/                  WBP_Overlay, WBP_GlobeProgressBarBase 等
│
├── Plugins/UE_MCP_Bridge/   # UE-MCP (db-lyon) — 111 工具蓝图读写
└── Source/*.Target.cs        # V7, bOverrideBuildEnvironment, Unreal5_8
```

## C++ 继承树

```
ACharacter
├── AAuraCharacterBase (Abstract + IAbilitySystemInterface)
│   ├── AAuraCharacter              # 玩家 | 5 native 组件
│   └── AAuraEnemy                  # 敌人 | IEnemyInterface

APlayerController → AAuraPlayerController        # EnhancedInput + 鼠标高光
APlayerState     → AAuraPlayerState              # IAbilitySystemInterface + ASC

UAbilitySystemComponent → UAuraAbilitySystemComponent

UAttributeSet → UAuraAttributeSet
  Health: 50  / MaxHealth: 100   (DOREPLIFETIME_CONDITION, COND_None)
  Mana:   10  / MaxMana:   50    (同上)

UObject
├── UAuraWidgetController           # MVC Controller 基类
│   └── UOverlayWidgetController    # Health/Mana 多播委托绑定 (纯 C++)
└── UAuraUserWidget                 # MVC View 基类

AActor → AAuraEffectActor
  TArray<FEffectBucket> EffectBuckets;  # Instant / Duration / Infinite 分桶
```

## 蓝图资产完整清单

### Actor 蓝图

| 蓝图 | 父类 | 组件数 | EventGraph | 特殊逻辑 |
|---|---|---|---|---|
| BP_FireArea | AuraEffectActor | 4 (Box+2Niagara) | 7节点 2流 | Box重叠→父类OnOverlap|
| BP_HealthCrystal | AuraEffectActor | 3 (CrystalMesh×0.2+Capsule) | 3节点 | Capsule重叠→OnOverlap→Destroy|
| BP_ManaCrystal | AuraEffectActor | 3 (CrystalMesh+Sphere×0.66) | 3节点 | Sphere重叠→OnOverlap→Destroy|
| BP_HealthPotion | AuraEffectActor | 3 (PotionMesh×0.2+Sphere) | **14节点** | ⚡ 蓝图完整GAS管线 |
| BP_ManaPotion | AuraEffectActor | 3 (PotionMesh×0.2+Box×0.7) | 6节点 | Box重叠→OnOverlap→Destroy|

### BP_HealthPotion — 唯一的蓝图 GAS 完整管线 (14 节点)

```
Sphere.OnComponentBeginOverlap
  then ────→ OnOverlap(TargetActor=OtherActor)     # C++ 虚函数
  then ────→ GetASC(OtherActor)                     # 获取 ASC
  data ────→ self→MakeEffectContext(ASC)            # 创建效果上下文
  return ──→ Context→MakeOutgoingSpec(GE_Class)    # 创建效果规格
  return ──→ SpecHandle→IsValid                     # 有效性检查
  true ────→ ApplyGameplayEffectSpecToSelf(ASC, Spec) # 应用到目标
  then ────→ Destroy Actor                          # 拾取后自毁
```

> 其他 Actor 蓝图直接调用父类 `OnOverlap(TargetActor)` + `Destroy Actor` 的简单模式。

### 角色蓝图

| 蓝图 | 继承链 | 组件 |
|---|---|---|
| BP_Aura_Character | AuraCharacter | SpringArm+Camera + 5 native |
| BP_EnemyBase | AuraEnemy | 6 native (ASC+SkeletalMesh×2+Capsule+Movement+Arrow) |
| BP_Goblin_Slingshot | BP_EnemyBase→AuraEnemy | 6 native |
| BP_Goblin_Spear | BP_EnemyBase→AuraEnemy | 6 native |

### GE 数据资产 (5 个, 纯 GameplayEffect)

| GE | Duration | Modifier 目标 |
|---|---|---|
| GE_FireArea | HasDuration | 持续火焰伤害 |
| GE_PotionHeal | Instant | +Health |
| GE_PotionMana | Instant | +Mana |
| GE_CrystalHeal | Instant | +Health |
| GE_CrystalMana | Instant | +Mana |

### UI 控件树

```
WBP_Overlay (CanvasPanel)
├── WBP_ManaGlobe_C      ← 锚定底部偏左 (X=-800)
└── WBP_HealthGLobe_C    ← 锚定底部偏右 (X=547)

WBP_GlobeProgressBarBase (SizeBox 250×250)
└── Overlay_Root (3层)
    ├── Image_Background (GlobeRing.png)
    ├── ProgressBar_Globe (MI_HealthGlobe/MI_ManaGlobe)
    └── Image_Glass (MI_EmptyGlobe)
```

### 其余蓝图

| 蓝图 | 父类 | EventGraph |
|---|---|---|
| BP_AuraGameMode | AuraGameModeBase | 空 (2 禁用事件) |
| BP_AuraPlayerController | AuraPlayerController | 空 (2 禁用事件, Tick=enabled) |
| BP_AuraPlayerState | AuraPlayerState | 空 (2 禁用事件, 含原生ASC) |
| BP_AuraHUD | AuraHUD | 空 (3 禁用事件, Tick=enabled) |
| BP_OverlayWidgetController | OverlayWidgetController | 空 (纯 C++ 委托) |

## 两种事件图模式

### 模式 A: 调用 C++ 父类 (Crystal / FireArea / ManaPotion)
```
绑定Overlap事件 ──then──→ OnOverlap(TargetActor) ──then──→ Destroy Actor
                          OtherActor ──→ TargetActor (数据线)
```

### 模式 B: 蓝图自实现 GAS (BP_HealthPotion)
```
绑定Overlap ──→ OnOverlap ──→ GetASC ──→ MakeEffectContext ──→ MakeOutgoingSpec
                                                                    │
                              Destroy Actor ←── Apply ←── IsValid ←┘
```

## MVC 数据流

```
UAuraAttributeSet::OnRep_Health()
  ↓ ASC::GetGameplayAttributeValueChangeDelegate
UOverlayWidgetController::HealthChanged(FOnAttributeChangeData)
  ↓ FOnHealthChangedSignature.Broadcast(NewHealth)
WBP_HealthGlobe → ProgressBar_Globe.SetPercent()
```

## 三种效果策略 (AuraEffectActor)

| 策略 | 重叠 | 离开 |
|---|---|---|
| Instant | ApplyEffectToSelf | 无操作 |
| HasDuration | ApplyEffectToSelf | 移除 ActiveHandle |
| Infinite | ApplyEffectToSelf | 再次重叠时移除 |

## UE 5.6→5.8 变更 + MCP

| 代码变更 | MCP 工具链 |
|---|---|
| `.Data.Get()`→`Data`, `.Def.Get()`→`Def` | Unreal_mcp v0.5.30: 已移除 |
| `#include "GameplayEffectTypes.h"` | UE 5.8 官方MCP: 已启用 |
| `DOREPLIFETIME_CONDITION_NOTIFY`→`DOREPLIFETIME_CONDITION` | **UE-MCP: ✅ 当前使用** |
| `Mixed`→`Full`, `ECC_Visibility`→`ECollisionChannel::ECC_Visibility` | 111 tools, WebSocket 49195 |
| `V5`→`V7`+`bOverrideBuildEnvironment`+`Unreal5_8` | |

