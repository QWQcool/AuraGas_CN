# AuraGas 学习记录

> 引擎: UE 5.6 → 5.8 | MCP: UE-MCP (db-lyon) | 最后更新: 2026-07-21

## 项目概述

AuraGas 是基于 Unreal Engine 的 RPG 游戏开发学习项目，核心围绕 **Gameplay Ability System (GAS)**，涵盖属性管理、效果系统、UI MVC 架构、多人网络同步和 MCP 工具链集成。

## 开发时间线

```
2026-02-10  第1-2章  项目初始化, 基础角色, Enhanced Input
2026-02-25  第2章    鼠标高光 + EnemyInterface
2026-02-26  第3-4章  GAS 属性系统 + 网络同步
2026-02-28  第5章    UMG UI + MVC 架构
2026-03-09  第5.8    重构优化
2026-03-11  第5.9    多人修复 → 第5章完结
2026-03~04  第6.1-3  EffectActor 重构 + 法力值调整
2026-04~05  第6.4-8  药水/水晶 GE + 多效果数组
2026-06      第6.9    无限效果重叠移除 → 第6章完结
2026-07-17           UE 5.6 → 5.8 升级
2026-07-21           MCP 工具链打通 → UE-MCP 成功读取蓝图
```

## 详细提交记录

### 第2章: 基础系统
- `f78f27c` Initial commit
- `e206f15` Base Character Class
- `c3cb0e1` 武器网格 + Enhanced Input
- `a59f4e1` 光标追踪敌人高光 + 控制器初始化
- `f613e6a` EnemyInterface: 高光/取消高光

### 第3-4章: GAS 系统
- `33c99a8` AbilitySystemComponent + AttributeSet
- `90e7c25` 属性系统完善 + 网络同步

### 第5章: UI 系统
- `e3822c3` UMG + CoreUObject, End 5.6
- `83a83af` End 5.7
- `7e35ef9` 重构 Aura 类, End 5.8
- `3058d73` Widget 回调扩展 + 修复多人崩溃, End 5.9

### 第6章: EffectActor 系统
- `abe1bb7` 重构 EffectActor, 移除碰撞组件
- `d54b8cc` 调整初始法力值 (Mana=10, MaxMana=50), End 6.3
- `71e816c` 血药/蓝药 GE 拾取, End 6.8
- `2f68b56` 无限效果重叠移除策略, End 6.9
- `cecbf3c` 按 Duration 分三组数组管理效果

### UE 5.8 升级 (2026-07-17~21)
- `0dbb3de` 资产结构整理 + 重定向器清理 + `.gitignore` 更新

## UE 5.6 → 5.8 升级全过程

### 6 处 C++ 代码修复
1. `AuraEffectActor.cpp`: `.Data.Get()` → `Data`, `.Def.Get()` → `Def`
2. `OverlayWidgetController.h`: 加 `#include "GameplayEffectTypes.h"`, 移除错误前向声明
3. `AuraAttributeSet.cpp`: `DOREPLIFETIME_CONDITION_NOTIFY` → `DOREPLIFETIME_CONDITION`
4. `AuraPlayerState.cpp`: `Mixed` → `Full`
5. `AuraPlayerController.cpp`: `ECC_Visibility` → `ECollisionChannel::ECC_Visibility`
6. `AuraGasEditor.Target.cs`: `V5`→`V7` + `bOverrideBuildEnvironment=true` + `IncludeOrderVersion=Unreal5_8`

### 升级坑点
- `BuildEnvironment=Unique` 不兼容安装版引擎 → 改用 `bOverrideBuildEnvironment=true`
- `FOnGameplayAttributeValueChange` 是 **delegate 类型** 不是 struct, 回调参数仍用 `FOnAttributeChangeData`
- `DOREPLIFETIME_WITH_PARAMS_FAST` 不接受 `{ }` lambda 语法, 直接用 `DOREPLIFETIME_CONDITION`
- 安装版引擎不支持 BuildEnvironment.Unique, 必须用 bOverrideBuildEnvironment

## MCP 工具链探索 (3 个阶段)

### 阶段 1: Unreal_mcp v0.5.30 (ChiR24) — 失败

- 安装: 克隆仓库 + npm 构建 TS 服务器 + 复制 C++ 插件
- 问题: `/Game/BP/` 目录的 Asset Registry 缓存不完整, 所有蓝图 NOT_FOUND
- 尝试 patch C++ 源码 (预扫描路径): 两次均失败
- 原因: v0.5.30 刻意移除 `ScanPathsSynchronous`, 纯靠预缓存
- 结论: 已完全移除

### 阶段 2: UE 5.8 官方 MCP — 半成品

- 启用: Unreal MCP + All Toolsets + MCP Client Toolset
- 发现: BlueprintTools 53 工具可通过 `describe_toolset` 查看
- 问题: `call_tool` 只能路由 AgentSkillToolset, 蓝图工具不可调用
- 原因: v1.0 实验版调用层未完成

### 阶段 3: UE-MCP (db-lyon) — ✅ 成功

- 安装: `git clone` → 复制 `UE_MCP_Bridge` C++ 插件 → 编译
- MCP 配置: `npx ue-mcp` + WebSocket 连接 `ws://127.0.0.1:49195`
- 工具: 111 个 action, 覆盖蓝图、UI、GAS、资产等 10+ 领域
- 核心能力:
  - `blueprint.read` — 组件树 (含 native) + 继承链
  - `blueprint.read_graph` — 节点级 (引脚/连线/默认值/坐标)
  - `widget.read_tree` — UMG 控件树 + anchor/slot
  - `blueprint.list_variables` — 变量名+类型+默认值
  - `blueprint.list_functions` — 函数签名+参数
  - `gas.*` — GAS 属性/效果/技能读写

### MCP 对比

| 工具 | 蓝图读 | C++编译 | 当前状态 |
|---|---|---|---|
| Unreal_mcp v0.5.30 | ❌ | 需编译 | 已移除 |
| UE 5.8 官方 MCP | ❌ (未接通) | 内置 | 已启用(占位) |
| **UE-MCP** | ✅ 111工具 | 需编译 | **当前使用** |

## `.gitignore` 管理

```
.vscode / .vs / .vsconfig    # VS IDE
.idea/                        # Rider IDE
.codebuddy/                   # CodeBuddy
*.sln                         # 所有解决方案 (含 Automation_*.sln)
Binaries/ / Intermediate/     # 编译产物
Saved/                        # 运行缓存
DerivedDataCache/             # DDC
```

## 已掌握技能清单

- UE5 C++ 体系: UCLASS / UPROPERTY / UFUNCTION / GENERATED_BODY
- Enhanced Input: IA + IMC + BindAction
- GAS: ASC + AttributeSet + GameplayEffect + DurationPolicy 三种策略
- UMG + MVC: HUD → WidgetController → UserWidget + 多播委托绑定
- 网络复制: ReplicatedUsing + DOREPLIFETIME_CONDITION + OnRep
- 引擎升降级: 5.6 → 5.8 API 迁移全流程
- MCP 工具链: 三种方案评测 + UE-MCP 部署 + CodeBuddy 集成
- 蓝图深度分析: MCP 读取变量/函数/事件图/控件树/引脚级连线

## 下一步方向

1. 第7章: GameplayAbility 技能系统 (施法/冷却/消耗)
2. 敌人 AI + 行为树
3. Niagara 粒子特效深度定制
4. 音效系统集成

---

*基于 MCP 工具自动分析 + git 提交历史 | 2026-07-21*
