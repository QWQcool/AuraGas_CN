# AuraGas 学习记录

> 引擎: UE 5.8 | 进度: 第8章完结（1~8 章完成），下一步 9.1 Attribute Menu

## 项目概述

基于 UE 5.8 的 RPG 学习项目（教程: Stephen Ulibarri 的 Gameplay Ability System 课程），核心围绕 **Gameplay Ability System (GAS)**。

## 开发时间线

```
2026-02-10  第1-2章  项目初始化, 角色, Enhanced Input, 武器网格, GameMode
2026-02-12  第2章    minifix
2026-02-25  第2章    光标追踪敌人高光 + EnemyInterface → 第2章完结
2026-02-25  第3章    GAS 核心: ASC + AttributeSet (3.6 End)
2026-02-26  第4章    属性系统完善 + 网络同步 (End 4.4)
2026-02-28  第5章    UMG + CoreUObject 依赖 (End 5.6)
2026-03-03  第5章    Widget 完善 (End 5.7)
2026-03-09  第5章    重构 + 指针优化 (End 5.8)
2026-03-11  第5章    多人修复 + Widget 回调扩展 (End 5.9) → 第5章完结
2026-03-23  第6章    EffectActor 重构 (6.1-6.2)
2026-04-15  第6章    法力值调整 + 药水/水晶 GE (end 6.3-6.8)
2026-07-17  第6章    无限效果重叠移除 + 按 Duration 分桶重构 (end 6.9)
2026-07-17~21        UE 5.6 → 5.8 升级 + MCP 工具链
2026-07-22            第6章完结: PreAttributeChange + EffectProperties + 拾取物动画
2026-07-23            第7章 7.1-7.8: GameplayTags 体系 + ASC EffectApplied
2026-07-23~24         第7章 7.9-7.16: 消息系统 + 幽灵球 + clamp 修复 → 第七章完结
2026-07-28            第8章 8.1-8.3: 原生 GameplayTags + 主/次/抗性/元属性定义
2026-07-29            第8章 8.4-8.9: AttributeBased 次级属性派生系统
2026-07-31            第8章 8.10-8.11: MMC 自定义计算 + 玩家状态系统 → 第8章完结
```

## 第1-2章: 项目初始化与基础系统

**视频范围**: 1.1 ~ 2.10（提交标记 `2.10 Game Mode Pre`）

### 2026-02-10 项目初始化
- `f78f27c` Initial commit / `1f65af3` Initial Project Files / `e206f15` The Base Character Class / `f6445e5` 删除 AuraCharacterBase 并完善 Copilot 会话记录
- 学习 UE5 项目标准目录结构、C++ 类创建、UCLASS 宏与反射系统

### 2026-02-11 输入系统 (2.10 Game Mode Pre)
- `c3cb0e1` 添加武器网格组件 + Enhanced Input 支持
- Enhanced Input vs 传统 InputSystem 对比、Input Action/Mapping Context 定义、`BindAction` 绑定

### 2026-02-25 鼠标高光检测 → 第2章完结
- `a59f4e1` 玩家光标追踪敌人高亮 + 角色控制器初始化
- `f613e6a` EnemyInterface: 继承该接口可被鼠标光标识别 + 自定义高光
- 技术要点: 射线检测 `CursorTrace()` 每帧执行、`LastActor/ThisActor` 状态管理、接口设计模式、`SetRenderCustomDepth` 高光

## 第3-4章: GAS 核心 (3.1-4.4)

### 第3章 GAS 集成 (3.6 End)
- `33c99a8` 集成 AbilitySystemComponent 与 AttributeSet + 模块依赖
- `AuraGas.Build.cs` 新增依赖: `GameplayAbilities`(Public) + `GameplayTags/GameplayTasks`(Private)
- `AAuraCharacterBase : ACharacter, IAbilitySystemInterface`，持有 ASC + AttributeSet

### 第4章 属性与网络同步 (End 4.4)
- `90e7c25` 完善角色能力与属性系统，支持网络同步与高亮交互
- 属性定义: Health/MaxHealth, Mana/MaxMana（`ATTRIBUTE_ACCESSORS` 宏简化访问器）
- 网络复制: `ReplicatedUsing = OnRep_Health` + `GetLifetimeReplicatedProps` + `DOREPLIFETIME_CONDITION`

## 第5章: UMG UI + MVC 架构 (5.1-5.9)

### 第5章推进
- `e3822c3` 新增 UMG 与 CoreUObject 依赖 (End 5.6)
- `83a83af` UI 控件完善 + 数据同步 (End 5.7)
- `7e35ef9` 重构 Aura 相关类 + 指针类型优化 (End 5.8)
- `3058d73` Widget 回调绑定扩展 + 优化属性/输入初始化 + **修复服务端运行多人游戏崩溃** (END 5.9 → 可开始 6.1)

### 核心架构: MVC
```
Model(数据) → Controller(逻辑) → View(显示)
AttributeSet    WidgetController    UserWidget
```
- `AuraHUD`: UI 管理器，`InitOverlay()` 创建 WidgetController + UserWidget
- `AuraWidgetController`: 基类，`FWidgetControllerParams` 参数结构体，`BroadcastInitialValues()` + `BindCallBacksToDependencies()`
- `OverlayWidgetController`: 多播委托 `OnHealthChanged/OnMaxHealthChanged/OnManaChanged/OnMaxManaChanged`
- 球型进度条: WBP_HealthGlobe / WBP_ManaGlobe（继承 WBP_GlobeProgressBarBase）

## 第6章: GameplayEffect 体系 (6.1-6.9) → 完结

### 6.1-6.5: EffectActor 基础
- `abe1bb7` 重构 AuraEffectActor，移除碰撞组件 + 新增效果应用方法
- `d54b8cc` 调整初始法力值 (Mana=10, MaxMana=50), end 6.3
- 三种 Duration 策略: Instant / HasDuration / Infinite，按 Duration 分桶管理效果数组
- `ApplyEffectToTarget / OnOverlap / EndOverlap` 流程

### 6.6-6.8: 药水/水晶/火焰区域
- `71e816c` 血药/蓝药通过 GE 实现玩家血量蓝量增加
- 5 种 GE 数据资产 + 5 种蓝图实现
- BP_HealthPotion 唯一蓝图完整 GAS 管线 (14 节点)
- BP_FireArea 火焰区域持续伤害 (HasDuration)

### 6.9: 第6章完结补充
**C++ 新增**:
- `UAuraAttributeSet::PreAttributeChange()` — 属性夹紧 [0, Max]
- `UAuraAttributeSet::SetEffectProperties()` — GE 回调上下文解析 (Source/Target ASC/Avatar/Controller/Character)
- `UAuraAttributeSet::PostGameplayEffectExecute()` — GE 后处理
- `TagsToAttributes TMap` — GameplayTag→Attribute 映射 (第7章准备)

**拾取物动画** (AAuraEffectActor):
- `bRotates` / `RotationRate` — 自旋转
- `bSinusoidalMovement` / `SineAmplitude` / `SinePeriodConstant` — 正弦浮动
- `Tick()` + `ItemMovement()` — 统一动画驱动
- `InitialLocation` — 浮动原点快照
- `ActorLevel` — 效果等级 (配合 CT_Potion 曲线表)

**新增资产**: `CT_Potion.uasset` — 药瓶治疗量随 ActorLevel 缩放的 CurveTable

## 第7章: GameplayTags + 消息系统 (7.1-7.16)

### 7.1-7.3: GameplayTags 配置

**Config/DefaultGameplayTags.ini** — 项目级标签定义:

| 标签 | 说明 |
|---|---|
| `Message.HealthCrystal` | 水晶治疗 |
| `Message.ManaCrystal` | 水晶回蓝 |
| `Message.HealthPotion` | 药瓶治疗 |
| `Message.ManaPotion` | 药瓶回蓝 |
| `Attributes.Vital.Health` | 生命值属性 |
| `Attributes.Vital.Mana` | 法力值属性 |
| `Abilities.Passive.ListenForEvent` | 被动监听 (未来) |
| `Cooldown.Arcane/Fire/Lightning.*` | 冷却标签 (未来) |
| `Event.Montage.*` | 动画蒙太奇 (未来) |
| `GameplayCue.*` | 视觉特效 (未来) |

**DT_PrimaryAttributes** — 主属性 DataTable，配合 `TagsToAttributes` TMap 做 O(1) 查询。

### 7.4-7.6: UAuraAbilitySystemComponent 扩展

```cpp
// 当 ASC 完全初始化时调用
void AbilityActorInfoSet()
{
    // 绑定 GE 应用回调
    OnGameplayEffectAppliedDelegateToSelf.AddUObject(this, &EffectApplied);
}

// GE 应用时提取 AssetTags 并屏显调试
void EffectApplied(ASC, EffectSpec, ActiveHandle)
{
    EffectSpec.GetAllAssetTags(TagContainer);
    for (Tag : TagContainer)
        GEngine->AddOnScreenDebugMessage(Tag.ToString());  // TODO → 广播到 UI
}
```

### 7.7-7.8: InitAbilityActorInfo 重构

```
AAuraCharacterBase::InitAbilityActorInfo()   [virtual, 空实现]
  └── AAuraCharacter::InitAbilityActorInfo() [override]
        ├── ASC->InitAbilityActorInfo(PlayerState, this)
        └── Cast<UAuraASC>(ASC)->AbilityActorInfoSet()  ← 新增

AAuraEnemy::BeginPlay()
  └── InitAbilityActorInfo()  [override]
        ├── ASC->InitAbilityActorInfo(this, this)
        └── AbilityActorInfoSet()  ← 新增
```

### 7.14: 拾取物提示消息系统

- `Message.*` AssetTags 通过 EffectApplied 回调广播到 WidgetController → Overlay UI 显示拾取提示。

### 7.15-7.16: 血蓝球幽灵球效果 + clamp 修复

- GlobeProgressBar 增加幽灵球滞后动画效果（主进度条 + 滞后尾迹）。
- 修复吃血药溢出 bug：数值同步 clamp 到 [0, Max] 区间。

## 第8章: 属性初始化与派生 (8.1-8.11)

### 8.1-8.3: 原生 GameplayTags + 属性分类

**AuraGameplayTags 单例** (`AuraGameplayTags.h/.cpp`, ~440 行原生标签注册):

| 分类 | 标签 |
|---|---|
| 主属性 | `Attributes.Primary.Strength/Intelligence/Resilience/Vigor` |
| 次级属性 | `Attributes.Secondary.Armor/ArmorPenetration/BlockChance/CritChance/CritDamage/CritResist/HealthRegen/ManaRegen/MaxHealth/MaxMana` |
| 抗性 | `Attributes.Resistance.Fire/Lightning/Arcane/Physical` |
| 元属性 | `Attributes.Meta.IncomingXP` |
| 伤害 | `Damage.Fire/Lightning/Arcane/Physical` |
| Debuff | `Debuff.Burn/Stun/Arcane/Physical` + Chance/Damage/Duration/Frequency |
| 技能 | `Abilities.Status.*` `Abilities.Type.*` `Abilities.Fire.FireBolt` 等 |
| 输入 | `InputTag.LMB/RMB/1/2/3/4` + `Player.Block.*` |
| 其他 | `CombatSocket.*` `Montage.Attack.*` `GameplayCue.FireBlast` |

**UAuraAttributeSet 属性体系**（20 个属性，全部 ReplicatedUsing + OnRep）:
- **Primary**: Strength / Intelligence / Resilience / Vigor
- **Secondary**: Armor / ArmorPenetration / BlockChance / CriticalHitChance / CriticalHitDamage / CriticalHitResistance / HealthRegeneration / ManaRegeneration / MaxHealth / MaxMana
- **Resistance**: Fire / Lightning / Arcane / Physical
- **Vital**: Health / Mana
- **Meta**: IncomingDamage / IncomingXP（不复制，仅服务端瞬时使用）

**DT_InitialPrimaryValues + GE_AuraPrimaryBlueprint**: 主属性初始值 DataTable + Instant GE 初始化。

### 8.4-8.9: AttributeBased 次级属性派生系统

初始化流程重构为 **Primary → Secondary** 两级 GE 链:

```
GE_AuraPrimaryBlueprint (Instant, 查表)   →  Strength/Intelligence/Resilience/Vigor
GE_SecondaryAttributes (Infinite, AttributeBased + Override, bSnapshot=false)
  → 12 个次级属性全部由主属性派生
```

**次级属性公式表**:

| 次级属性 | 公式 |
|---|---|
| Armor | 0.25 × (Resilience + 2) + 6 |
| ArmorPenetration | 0.15 × (Resilience + 1) + 16 |
| BlockChance | 0.25 × Armor + 4 |
| CriticalHitChance | 0.5 × ArmorPen + 10 |
| CriticalHitResistance | 0.25 × Armor + 10 |
| CriticalHitDamage | 0.25 × ArmorPen + 5 |
| HealthRegeneration | 0.1 × Vigor + 1 |
| ManaRegeneration | 0.1 × Intelligence + 1 |
| 四系抗性 | 0.5 × Resilience + 3 |

> 派生链非快照 (`bSnapshot=false`)：主属性变化时次级属性自动重算。
> 新增 **GE_VitalAttributes** (Instant + Override): Health=MaxHealth, Mana=MaxMana。
> 资产目录重构: `PrimaryAttributes/` → `DefaultAttributes/`。

### 8.10: MMC 自定义计算类

次级属性中的 **MaxHealth/MaxMana** 从 AttributeBased 升级为 **CustomCalculationClass**（自定义 ModifierMagnitudeCalculation）:

```cpp
// MMC_MaxHealth.h  (ModMagCalc/ 目录)
UCLASS()
class UMMC_MaxHealth : public UGameplayModMagnitudeCalculation
{
    GENERATED_BODY()
public:
    UMMC_MaxHealth();
    virtual float CalculateBaseMagnitude_Implementation(const FGameplayEffectSpec& Spec) const override;
private:
    FGameplayEffectAttributeCaptureDefinition VigorDef;
};

// MMC_MaxHealth.cpp — 捕获 Vigor，公式: 80 + 2.5×Vigor + 10×PlayerLevel
float UMMC_MaxHealth::CalculateBaseMagnitude_Implementation(const FGameplayEffectSpec& Spec) const
{
    FAggregatorEvaluateParameters EvaluationParameters{Spec.CapturedSourceTags, Spec.CapturedTargetTags};
    float Vigor = 0.f;
    GetCapturedAttributeMagnitude(VigorDef, Spec, EvaluationParameters, Vigor);
    Vigor = FMath::Max(Vigor, 0.f);

    int32 PlayerLevel = 1;
    if (Spec.GetContext().GetSourceObject()->Implements<UCombatInterface>())
        PlayerLevel = Cast<ICombatInterface>(...)->GetPlayerLevel();

    return 80.f + 2.5f * Vigor + 10.f * PlayerLevel;
}
```

**公式一览**:
- **MMC_MaxHealth** = 80 + 2.5 × Vigor + 10 × Level
- **MMC_MaxMana** = 50 + 2.5 × Intelligence + 15 × Level

**ICombatInterface** (`Interaction/CombatInterface.h`): 新增 `virtual int32 GetPlayerLevel()`，MMC 通过 `Spec.GetContext().GetSourceObject()` 强转获取玩家等级参与计算。Character/Enemy 均实现。

### 8.11: 玩家状态系统 (AAuraPlayerState)

**ULevelUpInfo (DataAsset)** (`AbilitySystem/Data/`):

```cpp
USTRUCT(BlueprintType)
struct FAuraLevelUpInfo
{
    int32 LevelUpRequirement;   // 升到该级所需经验
    int32 AttributePointAward;  // 升级奖励属性点
    int32 SpellPointAward;      // 升级奖励技能点
};

// 提供 FindLevelForXP(XP)：循环查表返回当前等级
int32 ULevelUpInfo::FindLevelForXP(int32 XP) const;
```

**AAuraPlayerState**（4 个复制状态字段）:

| 字段 | 默认值 | DOREPLIFETIME | OnRep 委托 |
|---|---|---|---|
| `Level` | 1 | ✅ | `OnLevelChangedDelegate(Level, bLevelUp)` |
| `XP` | 0 | ✅ | `OnXPChangedDelegate` |
| `AttributePoints` | 0 | ✅ | `OnAttributePointsChangedDelegate` |
| `SpellPoints` | 0 | ✅ | `OnSpellPointsChangedDelegate` |

- `AddTo*` / `Set*` 方法 + 对应多播委托广播，供 MMC/UI 使用。
- 持有 ASC (`UAuraAbilitySystemComponent`, Full 复制模式) + AttributeSet。
- `SetNetUpdateFrequency(100.f)`。

**最终初始化链 (8.11)**:

```
AAuraCharacterBase::InitializeDefaultAttributes()
  ├── ApplyEffectToSelf(DefaultPrimaryAttributes)    → GE_AuraPrimaryBlueprint (Instant)
  ├── ApplyEffectToSelf(DefaultSecondaryAttributes)  → GE_SecondaryAttributes (Infinite, MMC)
  └── ApplyEffectToSelf(DefaultVitalAttributes)      → GE_VitalAttributes (Instant)
```

## 章节内容总览

| 章节 | 内容 | 状态 |
|---|---|---|
| 1-2 | 项目初始化 + 角色 + Enhanced Input + EnemyInterface | ✅ |
| 3-4 | GAS 核心: ASC, AttributeSet, 网络同步 | ✅ |
| 5 | UMG + MVC + 球型进度条 | ✅ |
| 6 | GE 全体系: EffectActor, 药水/水晶/火焰, 拾取物动画 | ✅ |
| 7 | GameplayTags + 消息系统 + 幽灵球 + clamp 修复 | ✅ |
| **8** | **属性初始化 + 派生链 (AttributeBased/MMC) + 玩家状态** | **✅** |
| 9 | Attribute Menu: 属性点分配 UI | 下一步 |

## 详细提交记录

```
b0447d1  feat(GAS): MMC 自定义计算 + 玩家状态系统 (8.10-8.11)
ad4565d  feat(GAS): 基于属性的次级属性派生系统 (8.9)
74669a9  feat(ability-system): 添加主、次、抗性及元属性与游戏标签 end8.3mid
77c018f  feat(UI): 血蓝球幽灵球效果 + 修复吃血药溢出 bug (第七章完结)
82762fd  feat(UI): 实现拾取物提示消息系统 (GAS 7.14)
7742adf  ch7.1-7.8-gameplaytags-complete
b06a49c  ch6-complete-pre-ch7-tags
cecbf3c  Refactor AuraEffectActor to use per-duration-type effect arrays
2f68b56  feat(AuraEffectActor): 实现无限效果的重叠移除策略-end6.9
eecf624  feat: 增强聊天历史和IDE配置，新增多字段与组件
71e816c  新增血药和蓝药的通过GE实现玩家血量蓝量增加
d54b8cc  feat: 调整初始法力值，优化对话记录与效果组件逻辑,end 6.3
abe1bb7  feat: 重构AuraEffectActor，移除碰撞组件并新增效果应用方法
3058d73  feat: 扩展Widget回调绑定并优化属性与输入初始化, END 5.9
7e35ef9  refactor: 重构Aura相关类并清理Copilot对话记录 End 5.8
83a83af  End 5.7
e3822c3  feat: 新增UMG与CoreUObject依赖 END 5.6
90e7c25  feat: 完善角色能力与属性系统，支持网络同步与高亮交互, End 4.4
33c99a8  feat: 集成能力系统组件与属性集并添加依赖, 3.6 End
f613e6a  敌人接口,继承该接口可以被鼠标光标识别并且自定义高光
a59f4e1  feat: 实现玩家光标追踪敌人高亮与角色控制器初始化,第二章内容完结
c3cb0e1  feat: 添加武器网格组件及增强输入支持, 2.10 Game Mode Pre
f6445e5  refactor: 删除AuraCharacterBase并完善CopilotChatHistory会话记录
e206f15  The Base Character Class
1f65af3  Initial Project Files
f78f27c  Initial commit
```

## 技术要点

### 基础 C++
- UE 反射: `UCLASS/UPROPERTY/UFUNCTION/GENERATED_BODY`
- `TObjectPtr` vs 裸指针/智能指针
- 委托: `DECLARE_MULTICAST_DELEGATE_OneParam` + `Broadcast/AddUObject`
- 模块依赖: `PublicDependencyModuleNames` vs `PrivateDependencyModuleNames`

### GAS 进阶
- GameplayTags: `DefaultGameplayTags.ini` 项目级定义 + 原生单例 `FAuraGameplayTags::InitializeNativeGameplayTags()`
- 属性分类: Primary → Secondary → Vital 三级体系 + Meta/Resistance 辅助
- GE Magnitude 计算方式: **SetByCaller / AttributeBased / CustomCalculationClass(MMC)** 三种
- MMC 模板: `UGameplayModMagnitudeCalculation` + `FGameplayEffectAttributeCaptureDefinition` + `GetCapturedAttributeMagnitude()`
- `ICombatInterface::GetPlayerLevel()`: 接口解耦，MMC 与 Actor 通信
- `FGameplayTagContainer`, `FGameplayEffectSpec::GetAllAssetTags()`
- `OnGameplayEffectAppliedDelegateToSelf` 委托绑定
- GE 的 AssetTags vs GrantedTags: Instant GE 用 AssetTags 做消息通知

### 状态与复制
- PlayerState 持有 ASC + AttributeSet，4 个玩家状态字段全部 DOREPLIFETIME + OnRep 委托
- `DOREPLIFETIME` 网络复制 + 委托广播（服务端改值 → 全端刷新 UI）

### 架构重构
- C++ 虚函数: `InitAbilityActorInfo()` 在基类声明 virtual，子类 override
- ASC 初始化串: `InitAbilityActorInfo` → `AbilityActorInfoSet` → 绑定 EffectApplied
- 属性初始化串: `Primary → Secondary → Vital` 三级 GE 链
- DataAsset 查表: `ULevelUpInfo::FindLevelForXP()` 循环查经验表

## 下一步

1. **9.1 Attribute Menu** — 属性菜单 UI（属性点分配、升级加点）
2. 技能系统 GameplayAbility（法术技能、冷却、输入标签绑定）
3. 敌人 AI + 行为树

---

*MCP + git 分析 | 2026-07-31*
